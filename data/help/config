Your config file is generally in ~/.ldss/config.json

The following are properties you can set:
   Language            LANG to use if not specified
   WebPort             TCP port for ldss web server

The following are switches you can use to temporarily override certain properties:
   -l LANG             Language
   -p NUMBER           WebPort
